describe("Data type", () => {
  it("Should must declare", () => {
    let name: string = "Esa Dimas Aditya";
    let balance: number = 10000;
    let isValid: boolean = true;

    console.log(name);
    console.log(balance);
    console.log(isValid);

  });
});
